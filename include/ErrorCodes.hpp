#ifndef ERRORCODES_HPP
#define ERRORCODES_HPP

typedef enum ERRORCODE { OK=0,
                  UNKNOWN_CMDARGUMENT=1,
                  FILE_ACCESS,
                  BAD_STREAM,
} ERRORCODE;

#endif
